
const add = (x, y) => +x + +y
const divide = (x, y) => +x / +y
const multiply = (x, y) => +x * +y
const subtract = (x, y) => +x - +y
const exp = (x, y) => 8

module.exports = { add, divide, multiply, subtract, exp }